function buildPage() {

    // Creates DOM elements
    const mainElement = document.createElement("div");
    mainElement.id = "mainElement";
    mainElement.innerHTML = "HELLO THERE !";

    // Links DOM elements


    document.body.appendChild(mainElement);

    // Adds events
    
}

export { buildPage };